<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fithub"
        tools:targetApi="31">
        <activity
            android:name=".menu.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu.login.RegistreActivity" />
        <activity android:name=".menu.BaseActivity" />
        <activity android:name=".menu.main.AdminActivity" />
        <activity android:name=".menu.main.ClientActivity" />
        <activity android:name=".menu.usuari.PerfilActivity" />
        <activity android:name=".menu.activitats.ActivitatsActivity" />
        <activity android:name=".menu.serveis.ServeisActivity" />
        <activity android:name=".menu.installacions.InstallacionsActivity" />
        <activity android:name=".menu.reserves.ReservesPerDiaActivity" />
        <activity android:name=".menu.reserves.ReservesPerNomActivity" />
    </application>

</manifest>